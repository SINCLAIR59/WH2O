// ============================================
// 🎨 APP_COLORS.DART - สีและค่าคงที่
// ============================================
// ไฟล์นี้เก็บสีและค่าคงที่ที่ใช้ทั้งแอป
// ข้อดี:
// 1. แก้สีที่เดียว สีทั้งแอปเปลี่ยน
// 2. ไม่ต้องจำรหัสสี
// 3. มีชื่อสื่อความหมาย (primaryBlue ดีกว่า 0xFF0077BE)
// ============================================

import 'package:flutter/material.dart';

/// คลาสเก็บสีทั้งหมดของแอป
class AppColors {
  // ==========================================
  // สีหลัก (Primary Colors)
  // ใช้สำหรับ Header, Button, สีหลักของแอป
  // ==========================================
  static const Color primaryBlue = Color(0xFF0077BE);      // สีน้ำเงินหลัก
  static const Color primaryBlueDark = Color(0xFF005A8D);  // สีน้ำเงินเข้ม
  static const Color background = Color(0xFFF5F7FA);       // สีพื้นหลัง

  // ==========================================
  // สีสถานะ (Status Colors)
  // ใช้แสดงสถานะคุณภาพน้ำ
  // ==========================================
  static const Color excellent = Color(0xFF00C853);  // เขียว - ดีเยี่ยม
  static const Color good = Color(0xFF64DD17);       // เขียวอ่อน - ดี
  static const Color fair = Color(0xFFFFAB00);       // เหลือง - พอใช้
  static const Color poor = Color(0xFFFF6D00);       // ส้m - แย่
  static const Color critical = Color(0xFFD50000);   // แดง - วิกฤต

  // ==========================================
  // Gradient สี (Color Gradients)
  // ใช้สำหรับพื้นหลัง, การ์ด ให้ดูสวยขึ้น
  // ==========================================
  
  /// Gradient สีน้ำเงิน - ใช้กับ Header
  static const List<Color> primaryGradient = [
    Color(0xFF0077BE),
    Color(0xFF005A8D),
  ];

  /// Gradient สีเขียว - สถานะดีเยี่ยม
  static const List<Color> excellentGradient = [
    Color(0xFF00C853),
    Color(0xFF00E676),
  ];

  /// Gradient สีเขียวอ่อน - สถานะดี
  static const List<Color> goodGradient = [
    Color(0xFF64DD17),
    Color(0xFF76FF03),
  ];

  /// Gradient สีเหลือง - สถานะพอใช้
  static const List<Color> fairGradient = [
    Color(0xFFFFAB00),
    Color(0xFFFFD600),
  ];

  /// Gradient สีส้ม - สถานะแย่
  static const List<Color> poorGradient = [
    Color(0xFFFF6D00),
    Color(0xFFFF9100),
  ];

  /// Gradient สีแดง - สถานะวิกฤต
  static const List<Color> criticalGradient = [
    Color(0xFFD50000),
    Color(0xFFFF1744),
  ];
}

/// คลาสเก็บค่าคงที่ต่างๆ ของแอป
class AppConstants {
  // ==========================================
  // ระยะเวลา (Durations)
  // ==========================================
  
  /// ระยะเวลาอัพเดทข้อมูลอัตโนมัติ (ทุก 10 วินาที)
  static const Duration autoUpdateInterval = Duration(seconds: 10);

  /// ระยะเวลา Animation หลัก (1.5 วินาที)
  static const Duration animationDuration = Duration(milliseconds: 1500);
  
  /// ระยะเวลา Tween Animation (0.8 วินาที)
  static const Duration tweenDuration = Duration(milliseconds: 800);

  // ==========================================
  // ขนาด (Sizes)
  // ==========================================
  
  /// มุมโค้งของการ์ด (20 pixels)
  static const double cardRadius = 20.0;
  
  /// มุมโค้งของ Container ใหญ่ (30 pixels)
  static const double containerRadius = 30.0;

  // ==========================================
  // ระยะห่าง (Spacing)
  // ==========================================
  
  /// Padding มาตรฐาน (20 pixels)
  static const double defaultPadding = 20.0;
}